#include "myl.h"

int main()
{
    int v,e; 
    float f;                                                 
    int number_bytes;                                       
    printStr((char *)"Enter a integer: ");

    // -------------------------------------------------------------
    e = readInt(&v);                                      
   	printInt(e);
   	printStr((char *)"\n");

   	e = readInt(&v);
   	printInt(e);
   	printStr((char *)"\n");
   	number_bytes = printInt(v);
   	printStr((char *)"\n");
    printStr((char *)"printIntng this integer required "); 
    printInt(number_bytes);
    printStr((char *)" characters to be printed\n\n");                

   	e = readInt(&v);
   	printInt(e);
   	printStr((char *)"\n");
   	printInt(v);
   	printStr((char *)"\n");

   	// -------------------------------------------------------
    e = readFlt(&f);                                      
   	printInt(e);
   	printStr((char *)"\n");

   	e = readFlt(&f);
   	printInt(e);
   	printStr((char *)"\n");
   	number_bytes = printFlt(f);
   	printStr((char *)"\n");
    printStr((char *)"printIntng this float required "); 
    printInt(number_bytes);
    printStr((char*)" characters to be printed\n\n");                

   	e = readFlt(&f);
   	printInt(e);
   	printStr((char *)"\n");
   	printFlt(f);
   	printStr((char*)"\n");
    
    
    return 0;
}
